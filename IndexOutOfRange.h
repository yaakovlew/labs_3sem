#pragma once
#include <string>

class IndexOutOfRangeexeption {
public:
    std::string er = "ERROR, index need to be smaller then quantity and bigger then zero";
    std::string er1 = "ERROR, index need to be biggest then zero and smaller and equailly then length";
    std:: string er2 ="ERROR, startindex need to be smaller or equailly then endindex";
};